<center>
<% if @user.id == current_user.id %>
    <h1>Your Profile</h1>
<% else %>
    <h1>@<%= @user.username %>'s Profile</h1>
    <% if user_signed_in? %>
    <% if current_user.following.include?(@user.id) %>
        <p><%= link_to "Unfollow", unfollow_path(id: @user.id), class: "btn btn-danger following" %></p>
    <% else %>
        <p><%= link_to "Follow", now_following_path(id: @user.id), class: "btn btn-primary following" %></p>
    <% end %>
    <% end %>
<% end %>
</center>
<div class="bio">
<center><h3><%= @user.name %></h3></center>
<p><%= @user.bio %></p>
<div class="row">
    <center>
    <p><%= link_to "@#{@user.username}'s Books", user_books_path(id: @user.id), class: "btn btn-default" %></p>
    <p><%= link_to 'Follows', following_path(id: @user.id), class: "btn btn-default" %>
    <%= link_to 'Followers', followers_path(id: @user.id), class: "btn btn-default" %></p>
    </center>
</div>

</p>
</div>
<br>
<div class="row">
<% @user.posts.each do |post| %>
    <div class="col-xs-6">
    <div class="post">
        <p><%= post.message %></p>
    </div>
    <br>
    </div>
<% end %>
</div>